# The Movie DB project for nextion

Riverpod was chosen as the state management package for this project because of its features such as flexibility, caching and dependency injection.

JSON serializable for handling the to json and from json of the models.

Dio as its fetching package.

Very good analysis to enforce stricter linter rules and consistency.

Shared preferences was chosen because of its simple to use key value storage.